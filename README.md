racket-ga
=========

This is a parallel genetic algorithm implementation in Racket Scheme. It is written in a functional style and uses elitism, inversion, and subpopulations. Different populations run in separate threads, using Racket's *places* feature.